schema_version = 1

[package]
name = "opencv"

[source]
kind = "git"
url = "https://github.com/opencv/opencv.git"
strip_prefix = "opencv-latest"

[build]
system = "cmake"
exclude = ["samples"]
configure_args = []
build_args = []
install_args = []
